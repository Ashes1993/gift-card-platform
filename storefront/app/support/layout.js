export const metadata = {
  title: "پشتیبانی و تماس با ما | فروشگاه نکست لایسنس",
  description: "ارسال تیکت پشتیبانی، پیگیری سفارشات و ارتباط با واحد فروش.",
  robots: {
    index: true,
    follow: true,
  },
};

export default function SupportLayout({ children }) {
  return <>{children}</>;
}
